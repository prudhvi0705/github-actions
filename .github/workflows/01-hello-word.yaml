name: Hello World Workflow
on: [push]
  # push:
  #   branches:
  #     - main
jobs:
  hello:
    runs-on: ubuntu-latest
    steps:
      - name: Say Hello
        run: echo "Hello World from GitHub Actions 🚀"
